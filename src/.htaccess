<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Rutas válidas para el enrutador
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]
</IfModule>